# Market Sources Configuration
# Add repositories here to collect skills from the marketplace

# GitHub host configuration
# Change this if you're using a GitHub Enterprise instance or other GitHub-compatible host
github_host: "github.com"

sources:
  # Test source (using local skill-creator for testing)
  - repo: "zhangCan112/python-openskills"
    branch: "main"
    skillspath: "skills"
  
  # Add your repositories below
  # - repo: "owner/repo"
  #   branch: "main"
  #   skillspath: "skills"  # Single path
  
  # Or specify multiple paths
  # - repo: "owner/repo"
  #   branch: "main"
  #   skillspath:
  #     - "skills"
  #     - "custom-skills"
  
  # If skillspath is not specified, the entire repository will be searched
  # - repo: "owner/repo"
  #   branch: "main"